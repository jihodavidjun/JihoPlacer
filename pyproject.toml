[project]
name = "pineplace"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "torch",
    "numpy",
    "scipy",
    "tqdm",
    "absl-py",
    "matplotlib",
    "networkx",
    "pandas",
]
